NODE fastest_games_1
SQL >

    SELECT
        username,
        anyState(game_id) AS game_id,
        sumMergeState(game_length) AS game_length
    FROM games_mv
    GROUP BY username
    ORDER BY game_length ASC
    LIMIT 1 BY username


