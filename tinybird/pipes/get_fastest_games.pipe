NODE get_fastest_games_0
SQL >

    SELECT
      username,
      game_id,
      sumMerge(game_length) AS game_length
    FROM games_mv
    GROUP BY username, game_id
    ORDER BY game_length ASC LIMIT 1 by username


